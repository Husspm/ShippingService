server.port: 8181
spring:
  application.name: shipping-crud-service
  datasource:
    url: jdbc:mysql://localhost:3306/shipping?useSSL=false
    username: root
    password: rootroot
  jpa:
    hibernate.ddl-auto: create
    show-sql: true
